{% extends 'modal.html.twig' %}
{% block javascripts %}{{ encore_entry_script_tags('recette_form') }}{% endblock %}

{% block title %}Recette{% if activite is not null %} - {{ activite.nom }}{% endif %}{% endblock %}

{% block body %}
    {{ form_row(form.date) }}
    {% if kermesse is not null %}
        {{ form_row(form.activite) }}
        {{ form_row(form.report_stock) }}
    {% endif %}
    {{ form_row(form.libelle) }}
    {{ form_row(form.montant) }}
    {{ form_row(form.nombre_ticket) }}
{% endblock %}
