{% extends 'base.html.twig' %}

{% block title %}Membres actifs{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-5">
            <div class="well">
                <fieldset>
                    <legend><i class="fas fa-edit"></i> Membres actifs</legend>
                    {{ form_start(form) }}
                    {{ form_row(form.membres) }}
                    <button class="btn btn-primary" type="submit">Enregistrer !</button>
                    {{ form_end(form) }}
                </fieldset>
            </div>
        </div>
    </div>
{% endblock %}