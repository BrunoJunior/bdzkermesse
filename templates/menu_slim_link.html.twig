<a class="dropdown-item {% if link.active %}active{% endif %}" href="{{ link.href }}">{% if link.icon is not empty %}{% include "icon.html.twig" with {'icon': link.icon} only %} {% endif %}{{ link.name }}</a>