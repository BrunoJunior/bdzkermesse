{% extends 'base.html.twig' %}

{% block title %}Activité{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-5">
            <div class="well">
                <fieldset>
                    <legend><i class="fas fa-edit"></i> Activité</legend>
                    {{ form_start(form) }}
                    {{ form_row(form.nom) }}
                    {{ form_row(form.accepteTickets) }}
                    {{ form_row(form.nom) }}
                    <button class="btn btn-primary" type="submit">Enregistrer !</button>
                    {{ form_end(form) }}
                </fieldset>
            </div>
        </div>
    </div>
{% endblock %}